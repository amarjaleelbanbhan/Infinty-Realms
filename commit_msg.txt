feat: implement Phase 15 Asynchronous Guild Wars

- Added `GuildWar` model to Prisma schema to track war challenger, defender, target city, and siege points.
- Created `GuildWarModule`, `GuildWarController`, and `GuildWarService` in NestJS to handle declaring wars and contributing siege points.
- Implemented `GuildWarUI` on the client, adding a "Warfare" tab inside the existing `GuildUI.tsx`.
- Integrated active war fetching and point contribution for guild members.
